//function area(width: number, height: number): number {
    //return width * height;
//}
//let width: number = 5
//let height: number =5
//console.log(`พื้นที่ของสี่เหลี่ยมจตุรัส กว้าง $(width) สูง $(height) = ${area(width,height)} ตารางหน่วย`) 
//
//
//
//
//
const calculatArea = {width:number, height:number} => {
    return width * height;

}
let area: number = calculateArea(15,20);
console.log(area);